<br>
<div class="col-10 offset-2">
  <div class="container">

    <!-- column 1 -->
    <div class="a-8"><% image = find_piece(0, 7) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="a-7"><% image = find_piece(0, 6) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="a-6"><% image = find_piece(0, 5) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="a-5"><% image = find_piece(0, 4) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="a-4"><% image = find_piece(0, 3) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="a-3"><% image = find_piece(0, 2) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="a-2"><% image = find_piece(0, 1) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="a-1"><% image = find_piece(0, 0) %><%= image_tag(image, height: '65px').to_s if image %></div>

    <!-- column 2 -->
    <div class="b-8"><% image = find_piece(1, 7) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="b-7"><% image = find_piece(1, 6) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="b-6"><% image = find_piece(1, 5) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="b-5"><% image = find_piece(1, 4) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="b-4"><% image = find_piece(1, 3) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="b-3"><% image = find_piece(1, 2) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="b-2"><% image = find_piece(1, 1) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="b-1"><% image = find_piece(1, 0) %><%= image_tag(image, height: '65px').to_s if image %></div>

    <!-- column 3 -->
    <div class="c-8"><% image = find_piece(2, 7) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="c-7"><% image = find_piece(2, 6) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="c-6"><% image = find_piece(2, 5) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="c-5"><% image = find_piece(2, 4) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="c-4"><% image = find_piece(2, 3) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="c-3"><% image = find_piece(2, 2) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="c-2"><% image = find_piece(2, 1) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="c-1"><% image = find_piece(2, 0) %><%= image_tag(image, height: '65px').to_s if image %></div>

    <!-- column 4 -->
    <div class="d-8"><% image = find_piece(3, 7) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="d-7"><% image = find_piece(3, 6) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="d-6"><% image = find_piece(3, 5) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="d-5"><% image = find_piece(3, 4) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="d-4"><% image = find_piece(3, 3) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="d-3"><% image = find_piece(3, 2) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="d-2"><% image = find_piece(3, 1) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="d-1"><% image = find_piece(3, 0) %><%= image_tag(image, height: '65px').to_s if image %></div>

    <!-- column 5- -->
    <div class="e-8"><% image = find_piece(4, 7) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="e-7"><% image = find_piece(4, 6) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="e-6"><% image = find_piece(4, 5) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="e-5"><% image = find_piece(4, 4) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="e-4"><% image = find_piece(4, 3) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="e-3"><% image = find_piece(4, 2) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="e-2"><% image = find_piece(4, 1) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="e-1"><% image = find_piece(4, 0) %><%= image_tag(image, height: '65px').to_s if image %></div>

    <!-- column 6 -->
    <div class="f-8"><% image = find_piece(5, 7) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="f-7"><% image = find_piece(5, 6) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="f-6"><% image = find_piece(5, 5) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="f-5"><% image = find_piece(5, 4) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="f-4"><% image = find_piece(5, 3) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="f-3"><% image = find_piece(5, 2) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="f-2"><% image = find_piece(5, 1) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="f-1"><% image = find_piece(5, 0) %><%= image_tag(image, height: '65px').to_s if image %></div>

    <!-- column 7 -->
    <div class="g-8"><% image = find_piece(6, 7) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="g-7"><% image = find_piece(6, 6) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="g-6"><% image = find_piece(6, 5) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="g-5"><% image = find_piece(6, 4) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="g-4"><% image = find_piece(6, 3) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="g-3"><% image = find_piece(6, 2) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="g-2"><% image = find_piece(6, 1) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="g-1"><% image = find_piece(6, 0) %><%= image_tag(image, height: '65px').to_s if image %></div>

    <!-- column 8 -->
    <div class="h-8"><% image = find_piece(7, 7) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="h-7"><% image = find_piece(7, 6) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="h-6"><% image = find_piece(7, 5) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="h-5"><% image = find_piece(7, 4) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="h-4"><% image = find_piece(7, 3) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="h-3"><% image = find_piece(7, 2) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="h-2"><% image = find_piece(7, 1) %><%= image_tag(image, height: '65px').to_s if image %></div>
    <div class="h-1"><% image = find_piece(7, 0) %><%= image_tag(image, height: '65px').to_s if image %></div>

    <!-- a-h -->
    <div class="a">
      a
    </div>
    <div class="b">
      b
    </div>
    <div class="c">
      c
    </div>
    <div class="d">
      d
    </div>
    <div class="e">
      e
    </div>
    <div class="f">
      f
    </div>
    <div class="g">
      g
    </div>
    <div class="h">
      h
    </div>

    <!-- 1-8 -->
    <div class="1">
      1
    </div>
    <div class="2">
      2
    </div>
    <div class="3">
      3
    </div>
    <div class="4">
      4
    </div>
    <div class="5">
      5
    </div>
    <div class="6">
      6
    </div>
    <div class="7">
      7
    </div>
    <div class="8">
      8
    </div>
  </div>
</div>
